import { DiscountfilterPipe } from './discountfilter.pipe';

describe('DiscountfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
