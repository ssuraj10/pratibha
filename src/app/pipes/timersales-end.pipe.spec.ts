import { TimersalesEndPipe } from './timersales-end.pipe';

describe('TimersalesEndPipe', () => {
  it('create an instance', () => {
    const pipe = new TimersalesEndPipe();
    expect(pipe).toBeTruthy();
  });
});
